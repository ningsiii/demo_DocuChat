# requirements.txt   ← 只保留下面这些行，别再加其它奇怪包
streamlit

# LangChain 三件套（必须）
langchain~=0.3.25
langchain-community~=0.3.25
langchain-openai~=0.1.5      # 既给 OpenAI 也给 DeepSeek 用

# 通义千问 SDK
dashscope~=1.10.0             # 若 1.10.0 构建失败，可换 1.9.0 或干脆先注释

# 解析 PDF
pypdf

# 轻量向量库（替代 faiss-cpu，100% 纯 Python）
duckdb                        

# 常用数值库
numpy
